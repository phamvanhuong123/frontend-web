version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    command: ["npm", "run", "start"]

networks:
  default:
    driver: bridge
